name: Deploy to Streamlit Community Cloud

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Streamlit Deployment Note
        run: echo "✅ Kodu Streamlit'e manuel olarak deploy etmelisiniz: https://streamlit.io/cloud"
